version: 0.2
#version: 0.2

phases:
    install:
      finally:
        - echo 'Completed installing '
    build:
      commands:
        - echo 'Starting to move files'
        - aws s3 sync build s3://smoothflow-prod/forms
      finally:
        - echo 'DEMO UIs Deployement Succeded.'